/*

    - get all images from file using Path
    - store images in array
    - set active image 
    - render image
    - on arrow click :
        - move up or down by one index
    - on nav click:
        fetch index 
        move current active image to active index
        render screen

    on render:
        change image contained in div;
*/